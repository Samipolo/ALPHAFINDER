# AlphaFinder Pro v3 - runtime requirements
# The launcher prefers Python 3.13 when it is installed.

# Web framework
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1

# HTTP requests and scraping
requests>=2.32,<3
beautifulsoup4>=4.12,<5
lxml>=5.3,<7
httpx>=0.27,<1
cloudscraper>=1.2,<2
tradingview-ta>=3,<4
lse-data>=0.10,<1

# Financial data
yfinance>=0.2.57,<1
pandas>=2.2,<3
numpy>=2,<3

# Optional: Cloudflare bypass (install separately if needed)
# pip install cloudscraper

# Authentication + database
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9,<3
bcrypt>=4.1,<5